console.log("heyy!!");
 //With if / else if Statement.
 /*

var mark=prompt("Please enter the marks of student.");
let grade=function(){
if(mark<=50 )
{
    return ("B");
}
else if(mark<=100){
    return ("A");
}
}
let result=grade();
console.log(`Marks are ${mark} and grade is ${result}`);*/


  //With Switch case
  
/*
  var score=prompt("Please enter marks of student");
  switch(true) 
  {
    case (score<=20 && score<50):
          console.log(`Marks are ${score} and grade is B`);
        break;
    case (score>50 && score<=100):
            console.log(`Marks are ${score}  and grade is A`);
      break;
    default:(`Marks are ${score} and it is invalid`);
      break;
  }

*/
// With ternary 
/*
var mark=prompt("Enter marks");
let  getmarks=function(){
  return (mark<50 ?'B':'A');
}
let result=getmarks();
console.log(result);*/